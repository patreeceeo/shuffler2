# Absolute origin the app is deployed at, used for the Open Graph link-preview tags in
# index.html (crawlers do not resolve relative og:image URLs). Must end in a slash.
# Override per-deploy with a real environment variable or .env.local — this file is the
# default, not a secret.
VITE_SITE_URL=https://zzt64.com/shuffler2/
